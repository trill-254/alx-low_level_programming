#include <stdio.h>

/**
 * main - Prints numbers between 0 to 9 and letters between a to f.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 28; i < 38; i++)
	{
		putchar(i);
	}
	for (i = 103; i < 113; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
